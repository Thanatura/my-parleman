# Copy this file to config/metabase.env before starting Metabase.
# Use a random key with at least 16 characters.
MB_ENCRYPTION_SECRET_KEY=change-me-to-a-strong-random-key

# Optional tuning
MB_JETTY_PORT=3000
MB_APPLICATION_DB_MAX_CONNECTION_POOL_SIZE=15
