[project]
name = "parleman-tooling"
version = "0.1.0"
description = "Root tooling project for repository-wide developer utilities"
requires-python = ">=3.13,<3.14"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
]

[tool.uv]
package = false
